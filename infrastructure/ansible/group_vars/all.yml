# Ansible Variables - All Hosts
# =============================
# These are non-secret configuration values.
# Secrets are loaded from vault.yml (auto-generated by Terraform from secrets.yml).

# Git repository settings
# ⚠️  UPDATE THIS to your actual repository URL!
git_repo: "https://github.com/your-org/bisect-bot.git"
git_branch: main

# Application paths
app_path: /opt/bisect-bot
app_user: deploy
