version: "3.8"

# Simplified architecture: bisect runs directly in the bot container.
# No Docker-in-Docker, no Docker socket mount needed.
# Deploy multiple instances behind a load balancer for horizontal scaling.

services:
  # PostgreSQL database (shared across all instances)
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: bisect
      POSTGRES_USER: ${POSTGRES_USER:-bisect}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-bisect}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - backend
    restart: unless-stopped
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
      - DAC_READ_SEARCH
      - FOWNER

  # Unified bisect-bot service (web server + bisect execution)
  # Scale with: docker compose up -d --scale bot=5
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - GITHUB_APP_ID=${GITHUB_APP_ID}
      - GITHUB_APP_SLUG=${GITHUB_APP_SLUG:-bisect-bot}
      - GITHUB_PRIVATE_KEY_PATH=/app/secrets/private-key.pem
      - GITHUB_WEBHOOK_SECRET=${GITHUB_WEBHOOK_SECRET}
      - DATABASE_URL=postgresql://${POSTGRES_USER:-bisect}:${POSTGRES_PASSWORD:-changeme}@postgres:5432/bisect
      - BISECT_TIMEOUT_SECONDS=1800
      - MAX_CONCURRENT_JOBS=4
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
    volumes:
      # Mount the private key (read-only)
      - ./secrets:/app/secrets:ro
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - frontend
      - backend
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true

networks:
  frontend:
    driver: bridge
  backend:
    driver: bridge
    internal: true

volumes:
  postgres_data:
